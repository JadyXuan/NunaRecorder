# 实验分支：`requestConnectionPriority(HIGH)` 对 `connection_timeout(8)` 有没有用

**这个分支不合并、不发布，只在有人值守的实验窗口里用。**（T-2026-08-09-022 §3）

## 要回答的问题

2026-08-08 八台设备的诊断里，`gatt_status=8`（连接监督超时）横跨 4 台，
是**主导失败模式**。我们从来没有调过 Android 的连接优先级——设备每秒推约 50 个
20 ms 包，而默认 `BALANCED` 的连接间隔（约 30–50 ms）对这个速率偏松。

`CONNECTION_PRIORITY_HIGH` 把间隔压到 11.25–15 ms。**假设**：这会降低 `status=8` 的发生率。

**这只是假设。** 它也可能没有任何影响，甚至更差（更短的间隔更耗电、在拥挤的
2.4 GHz 环境里未必更稳）。实验的目的就是把它变成事实或者排除掉。

## 为什么单独成包

`AGENTS.md` §3：GATT 参数实验必须单独成包单独验证。GATT 的失败是静默的，
两个变量混在一个包里，出了问题分不清是谁干的——2026-08-06 卡死三天就是这么来的。

**这个分支相对 `feat/long-session-hardening` 只有两处 diff**：

1. `NunaBleLink.onConnectionStateChange` 里连上之后、`discoverServices()` 之前
   调一次 `requestConnectionPriority(CONNECTION_PRIORITY_HIGH)` 并打日志。
   这不是 GATT 队列操作（不占"一次一个未完成操作"的名额），是链路层参数更新。
2. `applicationIdSuffix = ".exp"`，包名 `com.example.nunarecorder.exp`。

第 2 处是**打包**上的，不改行为：如果实验包覆盖装在参与者包上，事后装回正式版
就是降级，Android 会要求先卸载，**而卸载会带走本地还没上传的录音**。
并排安装还顺带保证实验碰不到生产包的入组信息和会话目录。

## 怎么跑（约 30 分钟，需要人在旁边）

1. 装 `~/egoaudio-exp-connprio.apk`（sha256 `6f16d09e…`，版本名 `1.12-exp-connprio`）。
   桌面上会多一个图标，**和参与者那个 App 并存**，别搞混。
2. 拿一台**健康**设备（01AF 或 06EE）——要测的是链路参数，不是设备故障。
3. 连接 → 开始采集 → 等出流 → **走出房间直到断开** → 走回来等它自己重连。
   重复 10 次以上，尽量每次走到差不多远的地方。
4. 每次断开后看主页日志里的断开原因。记 `connection_timeout(8)` 出现了几次、
   总共断了几次。日志里还会有一行 `[实验] requestConnectionPriority(HIGH) = true`，
   **没有这一行说明装错包了**。
5. 用正式版（v1.12 / 13）**在同一地点、同样走法**再跑 10 次作为对照。
   没有对照组这个实验说明不了任何事情。

## 怎么算有效

`status=8` 在总断开次数里的占比明显下降，且重连成功率不变差。
样本只有十几次，只看得出大的差异；**如果两边差不多，结论就是"这个方向不对"**，
那也是有价值的结果——可以把注意力挪到别的地方（MTU、设备侧固件、天线遮挡）。

## 跑完之后

- 有效：把第 1 处改动 cherry-pick 进 `feat/long-session-hardening`，单独发一版。
- 无效：删掉这个分支，把结论写进 T-2026-08-09-022，别让下一个人再试一遍。

无论哪种，**记得把实验包从手机上卸掉**——它是 debug 包名，长期留着只会造成混淆。

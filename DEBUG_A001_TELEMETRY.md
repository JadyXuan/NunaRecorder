# 调试分支：A001 状态遥测（心跳 / 录音状态 / 佩戴 / 设备信息）

**这个分支不进 release。** 用户 2026-08-09：「可能不着急合并到我们的 release 版本，
你先调试着，ruihan 可能也会调试，等他通知我们再进一步迭代优化。」

## 已经搬过来的

从上游 `signed-test/recording-stop-recovery` 原样取用（Ruihan 实机验证过，不重写）：

- `ble/NunaProtocolInspector.kt` —— A001 状态报文解析：
  | 类型 | 含义 | 为什么值得要 |
  | --- | --- | --- |
  | `0x15` | 心跳（序号） | **分清"BLE 还连着但音频停了"和"设备整体没反应"**。今天所有的 `audio_stalled` 都无法区分这两种。 |
  | `0x11` | 录音状态 | 判断"App 已发 START 但设备并没有在录"。今晚那个状态机卡死，有这个字段就是一眼的事。 |
  | `0x05` | 设备信息 | 型号 / 固件 / 硬件版本 / 序列号，比标准 DIS 准。现在 manifest 里的 `device_firmware` 读的是 DIS。 |
  | `0x16` `0x17` | 佩戴状态与检测开关 | 没戴的时候音频缺失不该按故障告警。 |
  | `0x03` | 电量 | 与标准 BAS 组成回退链。 |
- `ble/NunaProtocolInspectorTest.kt`
- `docs/NUNA_PROTOCOL_VALIDATION.md` —— 协议能力评估与验证计划

137 项单元测试全绿（解析是纯函数，能直接测）。

## 还没做的（下一步）

1. **订阅 A001 NOTIFY**，把解析结果打进 `DiagnosticsLog`。
   要走现有的"一次一个 GATT 操作"纪律：A001 的 CCCD 写入必须排在 A003 订阅**之后**，
   和电量/固件读错开——2026-08-06 卡死三天就是两个 CCCD 挤在一起。
2. **把心跳放进 manifest 的诊断时间线**，和 `link.events`、音频停滞对齐。
   有了它才能回答"停流的时候设备还活着吗"。
3. **`0x05` 取代 DIS 作为 `device_firmware` 的来源**，DIS 保留为回退。

## 不要做的

`docs/NUNA_PROTOCOL_VALIDATION.md` 里已经列明：删除缓存 `0x06` 不可逆、
震动/雷达/佩戴检测开关会改变设备状态、A004 离线音频协议不完整。
这个分支只做**只读**遥测。

## 合并前提

等 Ruihan 通知。他手上有 Gavin 给的最新协议原稿，正在自己调；
两边同时改同一套解析只会制造冲突。

# NunaRecorder v0.1.0-beta.8

本版本补齐产品版 Nuna 的正式录音控制、停止恢复和完整音频分组边界处理，同时继续保留 XIAO nRF52840 自制版的兼容回退路径。

## 产品版 Nuna 兼容性修复

- 连接后主动协商较大的 ATT MTU，并在 A003 订阅完成后通过 A002 发送正式 START 控制命令。
- 以收到首个 A003 音频包作为“录制已开始”的依据，避免仅凭 CCCD 写入成功误报录制状态。
- 用户停止时发送正式 STOP 控制命令；停止后保持 GATT 连接，可在同一次连接中再次录制。
- 设备报告残留 `recording=true` 时，开始新会话前先发送 STOP 清理旧状态。
- START 成功但 8 秒内没有音频时执行一次 STOP 恢复，并自动重试一次。

## 音频完整性修复

- 产品固件可能在 App 未订阅时继续推进 A003，重新订阅会从 `chunk=1..5` 接入。App 现在先等待下一组 `chunk=0`，不会把残缺前导包写入新会话。
- 停止录制时继续接收当前组尾包，并在 `chunk=totalChunks-1` 后封口，避免生成不完整 Opus 分段。
- 无法按产品协议解析的旧 Nuna/XIAO 数据仍使用原有写入路径，并通过超时完成停止。
- 每个会话单独重置协议包序号和边界状态，诊断日志可定位首包、尾包和控制反馈。

## 状态机与诊断

- 增加独立的“正在停止录制”和“正在断开”状态，停止录制不再隐式断开设备。
- 增加独立断开按钮，录制停止后回到已就绪状态。
- 解析并记录 A001 设备信息、运行状态、电量、录音状态、雷达状态、佩戴状态、心跳和传感器报文。
- 记录 GATT 能力、MTU、控制响应、A003 Frame/Chunk 摘要和校验分类，方便定位固件差异。

## 当前实机验证进展

产品版设备（固件 `3.14.5.1736`、硬件 `HW1.1`）已完成同一 GATT 连接下两次连续录制：

- 第一段从 `chunk=3/6` 接入，丢弃残缺前导包后输出 612 个 Opus 帧，`integrity=ok`。
- 第二段从 `chunk=5/6` 接入，丢弃残缺前导包后输出 720 个 Opus 帧，`integrity=ok`。
- 两次停止均在完整分组末尾封口，全程没有 `status=8`、GATT 断开或重新握手。

已确认产品固件在 STOP 成功后仍可能保持 A001 `recording=true`，因此 App 保留下一会话开始前的预清理 STOP。XIAO nRF52840 的历史音频格式有自动化测试覆盖，但 beta.8 新增的“停止后再次开始”流程仍建议在 XIAO 实机上补充一次回归验证。

## 安装与隐私

下载 `NunaRecorder-v0.1.0-beta.8.apk`，在 Android 8.0 或更高版本上允许当前浏览器或文件管理器“安装未知应用”，然后打开 APK 安装。

使用上一正式签名 Beta 的用户可以直接覆盖升级，现有录音和设置会保留。公开 APK 不包含默认服务器、共享 Basic Auth 凭据或第三方 API Key；服务器上传默认关闭，需用户在设置中显式配置自己的服务。

请在采集他人声音前取得必要同意，并遵守所在地法律。

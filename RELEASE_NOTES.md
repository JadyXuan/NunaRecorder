# NunaRecorder v0.1.0-beta.3

本版本是针对原 Nuna 设备 BLE 音频缺口的兼容性测试版，并保留 beta.2 的默认隐私与自建服务器行为。

## 兼容性更新

- 单次 BLE 帧号跳变、分片异常或通知截断不再终止整场录制和断开设备。
- App 会继续保存后续音频，并在页面日志中显示具体缺口原因。
- 受影响的当前分段仍标记为不完整，不进入自动 VAD 和自动上传；后续正常分段可继续处理。
- 新增缺帧后继续录制与分段完整性的单元测试。

## 安装说明

下载 `NunaRecorder-v0.1.0-beta.3.apk`，在 Android 8.0 或更高版本上允许当前浏览器或文件管理器“安装未知应用”，然后打开 APK 安装。

使用上一正式签名 Beta 的用户可以直接覆盖升级，现有录音仍保留。公开 APK 不包含默认服务器、共享 Basic Auth 凭据或第三方 API Key；如需服务器功能，请在 App 设置中显式填写自己的地址、用户 ID 和可选凭据。

请在采集他人声音前取得必要同意，并遵守所在地法律。

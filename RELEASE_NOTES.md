# NunaRecorder v0.1.0-beta.5-test.1

本测试版按照最新协议补齐产品版 Nuna 的录音启动流程，并继续兼容 XIAO nRF52840 自制版。

## 录音启动与诊断

- BLE 连接后主动请求 MTU 517，为产品版约 501 字节的批量音频 Notify 留出空间。
- A003 通知订阅成功后，通过 A002 发送 `RECORDING_CTRL/START`，并按 command ID 追踪成功、错误码和超时。
- “录制已开始”现在以收到首个 A003 音频包为准，不再把 CCCD 写入成功误报为设备已经录音。
- 对 XIAO/旧 Nuna 保留“订阅 A003 后直接推流”兼容路径；即使没有控制反馈，只要收到音频仍可正常录制。
- 启动 8 秒仍无音频时返回可重试状态，并在日志中记录协商 MTU 与控制反馈状态。
- 缓存 CCCD 回调前抢先到达的少量音频包，避免丢失录音开头。

## 双设备兼容性更新

- 产品版 Nuna 可在一个 `frameId`/chunk 组内批量携带多个 80 字节 Opus 帧；App 现在按 80 字节整数倍写入，不再误报 2880/80 长度异常。
- XIAO nRF52840 自制版的单组单帧 `1 × 80 B` 格式保持兼容。
- 批量数据如果有不足 80 字节的尾部，仅丢弃不完整尾部并标记当前分段，完整 Opus 帧仍会保留。
- 单次 BLE 帧号跳变或分片异常仍不会终止整场录制；受影响分段不进入自动 VAD 和自动上传。
- 新增产品版 `6 × 480 B = 2880 B` 与 XIAO 版 `1 × 80 B` 回归测试。

## 安装说明

本版本先通过私有 GitHub Actions Artifact 提供，不创建公开 GitHub Release。使用同一正式签名，可通过 ADB 覆盖安装并保留现有数据。

使用上一正式签名 Beta 的用户可以直接覆盖升级，现有录音仍保留。公开 APK 不包含默认服务器、共享 Basic Auth 凭据或第三方 API Key；如需服务器功能，请在 App 设置中显式填写自己的地址、用户 ID 和可选凭据。

请在采集他人声音前取得必要同意，并遵守所在地法律。

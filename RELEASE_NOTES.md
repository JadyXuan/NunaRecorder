# NunaRecorder v0.1.0-beta.2

本版本重点改进默认隐私行为和自建服务器支持。设备录音、Nuna 协议及 XIAO nRF52840 Sense 兼容性保持不变。

## 主要更新

- 移除默认服务器地址和默认用户 ID。
- 新安装时生活记录、后台标注轮询和音频自动上传均关闭，不会连接业务服务器。
- 未配置有效 Base URL 和用户 ID 时，手动上传与后台任务会在本地拒绝，不发送网络请求。
- 升级时清除上一测试版隐式保存的试点服务器地址；若仍需使用该服务器，请在设置中重新填写。
- 设置页增加 Base URL 格式校验和更明确的联网提示。
- 新增自建服务器对接文档，覆盖认证、裸 Opus、音频上传、完整会话同步、时间轴、日记与标注 API。
- 移除公开源码和 APK 中曾存在的第三方调试转写凭据。

## 安装说明

下载 `NunaRecorder-v0.1.0-beta.2.apk`，在 Android 8.0 或更高版本上允许当前浏览器或文件管理器“安装未知应用”，然后打开 APK 安装。

使用上一正式签名 Beta 的用户可以直接覆盖升级，现有录音仍保留。公开 APK 不包含默认服务器、共享 Basic Auth 凭据或第三方 API Key；如需服务器功能，请在 App 设置中显式填写自己的地址、用户 ID 和可选凭据。

请在采集他人声音前取得必要同意，并遵守所在地法律。

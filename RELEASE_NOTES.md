# NunaRecorder v0.1.0-beta.4

本版本修复产品版 Nuna 和 XIAO nRF52840 自制版之间的 A003 音频分组格式差异，并保留 beta.3 的缺口容错行为。

## 双设备兼容性更新

- 产品版 Nuna 可在一个 `frameId`/chunk 组内批量携带多个 80 字节 Opus 帧；App 现在按 80 字节整数倍写入，不再误报 2880/80 长度异常。
- XIAO nRF52840 自制版的单组单帧 `1 × 80 B` 格式保持兼容。
- 批量数据如果有不足 80 字节的尾部，仅丢弃不完整尾部并标记当前分段，完整 Opus 帧仍会保留。
- 单次 BLE 帧号跳变或分片异常仍不会终止整场录制；受影响分段不进入自动 VAD 和自动上传。
- 新增产品版 `6 × 480 B = 2880 B` 与 XIAO 版 `1 × 80 B` 回归测试。

## 安装说明

下载 `NunaRecorder-v0.1.0-beta.4.apk`，在 Android 8.0 或更高版本上允许当前浏览器或文件管理器“安装未知应用”，然后打开 APK 安装。

使用上一正式签名 Beta 的用户可以直接覆盖升级，现有录音仍保留。公开 APK 不包含默认服务器、共享 Basic Auth 凭据或第三方 API Key；如需服务器功能，请在 App 设置中显式填写自己的地址、用户 ID 和可选凭据。

请在采集他人声音前取得必要同意，并遵守所在地法律。
